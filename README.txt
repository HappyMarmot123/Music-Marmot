Node 22.15.0
Docker 28.0.4

도커
docker build -t project-marmot .

git reset HEAD~1 --soft   // 푸시 되돌리기
git reveert HEAD~1 --soft   // 커밋 되돌리기
git push origin branch -f  // 강제 푸시
git stash                   // push, pop
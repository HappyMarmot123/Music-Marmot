Node 22.15.0
Docker 28.0.4

도커
docker build -t project-marmot .

git reset HEAD~1 --soft   // 푸시 되돌리기
git push origin branch -f  // 강제 푸시a
git stash                   // push, pop (LIFO)

트랙 플레이시 오디오 플레이어 드래그 프레임 드랍

